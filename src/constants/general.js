export const MOUSEMOVE_THROTTLE_INTERVAL = 20; // A bit less than 60 fps
export const MOUSE_CLICK_TOLERANCE = 10; // Tolerance of mouse movement in pixels during click (mousedown to mouseup)

export const MAX_TEXTURE_SIZE = 204800; // 200kb max

export const INPUT_SAVE_TIMEOUT = 300;
