export const HEX_SIZE = 40; // Hex side length in pixels

// Derived values
export const HEX_HEIGHT = HEX_SIZE * 2;
export const HEX_WIDTH = (Math.sqrt(3) / 2) * HEX_HEIGHT;

export const DEFAULT_GRID_WIDTH = 11;
export const DEFAULT_GRID_HEIGHT = 10;
